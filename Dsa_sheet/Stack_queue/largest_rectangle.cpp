#include<iostream>
#include<stack>
#include<vector>
using namespace std;
int largest_rectangle(vector<int>& arr){
    vector<int>(n,0);
    vector<int>(n,0);
}
int main(){
    vector<int> arr = {};
    cout<<"largest area rectangle is -> "<<largest_rectangle(arr);
    return 0;
}